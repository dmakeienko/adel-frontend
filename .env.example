# Backend API URL
# In development mode with proxy (npm run dev), leave this empty to use relative paths
# In production, set this to the actual backend URL (e.g., https://your-server:8080)
VITE_API_URL=
